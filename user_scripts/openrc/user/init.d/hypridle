#!/sbin/openrc-run

name="Hypridle"
description="Hyprland idle daemon"
supervisor=supervise-daemon
command="/usr/bin/hypridle"

depend() {
	need dbus
	after hyprland
}

export DBUS_SESSION_BUS_ADDRESS="unix:path=${XDG_RUNTIME_DIR}/bus"