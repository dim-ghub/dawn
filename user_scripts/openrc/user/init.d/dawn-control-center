#!/sbin/openrc-run

name="Dusky Control Center"
description="Dusky system control center daemon"
supervisor=supervise-daemon
command="${HOME}/user_scripts/dawn_system/control_center/dawn_control_center.py"

depend() {
	need dbus
	after hyprland
}

export DBUS_SESSION_BUS_ADDRESS="unix:path=${XDG_RUNTIME_DIR}/bus"