#!/sbin/openrc-run

name="SwayOSD"
description="OSD for volume/brightness on Wayland"
supervisor=supervise-daemon
command="/usr/bin/swayosd-server"

depend() {
	need dbus
	after hyprland
}

export DBUS_SESSION_BUS_ADDRESS="unix:path=${XDG_RUNTIME_DIR}/bus"